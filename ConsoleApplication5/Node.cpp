/*

#include "Card.h"
#include "Node.h"
#include <string>


Card* data;
Node* next;

Node::Node()
{
	data = null;
}

Node(Card* d);
Node(Card* d, Node* n);

//you most likely use setters & getters for the card and node or add a nested struct(class)
Card* getData();
void setData(Card* c);
Node* setNext();
void setNext(Node* n);
*/
